import React from 'react';
import PropTypes from 'prop-types';

const About = props => {
    return (
        <div className='footer'>
            <h3>This is from about components</h3>
        </div>
    );
};

About.propTypes = {
    
};

export default About;